scheme:[//[user[:password]@]host[:port]][/path][?query][#fragment]

cur 风格的是 test 类处理

参数类型的 需要重新写一个类
    1. 在servlet处指定处理类
    2. 这个处理类的handlers 需要实现 一个接口(还没写) *** 这个接口的作用是用于传递参数
    3. 在这个类的service方法中找到cmd , 如果没有就返回, 有就调用相应的方法( 因为cmd是string,所以相应的方法名写在数组中 )
       *** 这个类是这样设想的，实现一个接口，这个接口只有一个执行方法，每个实现类中实现这个方法，通过反射创建该类的实例
       *** 然后通过传过来的参数拿到 cmd 然后调用方法 并 放回结果